# Verify

Run:

```bash
python3 scripts/verify.py
```

The verifier checks file hashes, calendar continuity, statuses, and daily proof
identifiers. Aggregate derivations are also checked before publication.
