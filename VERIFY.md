# Verify

Run:

```bash
python3 scripts/verify.py --public-repo .
```

The verifier checks CSV schemas, date coverage, daily-to-weekly/monthly aggregation, public file hashes, and leak guards that block private/raw artifacts from being tracked.
